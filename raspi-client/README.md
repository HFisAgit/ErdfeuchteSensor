Dies ist ein Python client für den RasdpberryPi, der die Messdaten vom esp32 entgegennimmt und in einer Datei speichert.

Dieses Projekt dient als Beispiel, wie der Sensor in einem Raspi-System integriert werden könnte.